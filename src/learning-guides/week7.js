const rows=String.raw`ingestion|source = {"id": "doc-7", "revision": 2, "text": "  Returns accepted.  "}\nrecord = {"source_id": source["id"], "revision": source["revision"], "text": source["text"].strip()}\nprint(record)|{'source_id': 'doc-7', 'revision': 2, 'text': 'Returns accepted.'}|Loading a document is followed by normalization, provenance recording and indexing. Keeping source identity and revision enables replacement or deletion of stale chunks later. This example models that boundary without calling an index.|Appending a new revision without removing stale chunks can produce conflicting answers.|Design the keys needed to replace all chunks from revision 1 atomically.
chunking|words = "one two three four five six".split()\nsize, overlap = 3, 1\nchunks = [" ".join(words[start:start + size]) for start in range(0, len(words), size - overlap)]\nprint(chunks)|['one two three', 'three four five', 'five six']|The stride is size minus overlap. Repeating a boundary word retains context but consumes storage and prompt space more than once. Production token-based or semantic chunking must respect actual document structure.|Overlap equal to or larger than size makes this stride invalid.|Try overlap 0 and compare the boundary context, then validate the configuration.
metadata|chunks = [{"tenant": "a", "text": "allowed"}, {"tenant": "b", "text": "private"}]\ncaller_tenant = "a"\nvisible = [chunk["text"] for chunk in chunks if chunk["tenant"] == caller_tenant]\nprint(visible)|['allowed']|The trusted caller identity constrains which documents may become context. This in-memory filter illustrates the policy; a production index query should enforce equivalent access constraints before returning candidates.|Filtering only the final displayed answer is too late if private text already entered the model context.|Add document-level permissions in addition to tenant matching.
embeddings|vectors = {"cat": [1.0, 0.0], "kitten": [0.9, 0.1], "invoice": [0.0, 1.0]}\nquery = vectors["cat"]\nfor name in ["kitten", "invoice"]:\n    score = sum(a * b for a, b in zip(query, vectors[name]))\n    print(name, round(score, 2))|kitten 0.9\ninvoice 0.0|These hand-authored vectors illustrate the idea of a representation space; they are not learned embeddings and the dot products are not calibrated probabilities. A real encoder maps compatible queries and documents into the same versioned space.|Equal dimensions do not make vectors from different models meaningfully comparable.|Replace the toy vectors with one model's query and document encodings and compare rankings.
cosine|from math import sqrt\ndef cosine(a, b):\n    dot = sum(x * y for x, y in zip(a, b))\n    return dot / (sqrt(sum(x*x for x in a)) * sqrt(sum(y*y for y in b)))\nprint(round(cosine([1, 0], [4, 0]), 2))\nprint(round(cosine([1, 0], [0, 3]), 2))|1.0\n0.0|Dividing by the two magnitudes removes length from the comparison. Parallel nonzero vectors have similarity 1; perpendicular ones have 0. pgvector's cosine operator returns distance, so lower is better there.|The formula is undefined for a zero vector; production code must also check dimensions.|Add zero-vector and dimension checks before computing the score.
vector-search|query = [1, 0]\nrows = {"a": [0, 1], "b": [1, 0], "c": [2, 0]}\ndef squared_distance(vector):\n    return sum((a-b)**2 for a, b in zip(query, vector))\nranked = sorted(rows, key=lambda key: squared_distance(rows[key]))\nprint(ranked[:2])|['b', 'c']|This exact search uses squared Euclidean distance, so the closest row sorts first. pgvector performs the analogous ordering in SQL with a chosen distance operator and LIMIT. Always match the operator to your embedding/index design.|Sorting a distance descending returns the least similar results.|Write the equivalent pgvector query, then compare its result with this exact baseline.
vector-indexes|exact_top = {"a", "b", "c", "d"}\napproximate_top = {"a", "b", "x", "y"}\nrecall = len(exact_top & approximate_top) / len(exact_top)\nprint(recall)|0.5|Only two of the four exact neighbors survive this approximate result. An approximate index trades search work for potentially missed neighbors. Benchmark latency together with recall on representative data and filters.|Faster retrieval can still make the downstream answer worse if important evidence disappears.|Compare two index configurations using the same queries and exact baseline.
full-text|documents = {"a": "payment refund policy", "b": "shipping times"}\nquery_terms = {"refund"}\nmatches = [key for key, text in documents.items() if query_terms <= set(text.split())]\nprint(matches)|['a']|This toy term matcher illustrates lexical retrieval. PostgreSQL adds language-aware parsing, lexemes, query operators and ranking through tsvector and tsquery. Exact identifiers often benefit from lexical search even when semantic similarity is available.|Whitespace token matching is not a replacement for PostgreSQL full-text semantics.|Compare refund and refunds using an English to_tsvector configuration.
hybrid|lexical = ["a", "b"]\nsemantic = ["b", "c"]\nscores = {}\nfor ranking in [lexical, semantic]:\n    for rank, key in enumerate(ranking, start=1):\n        scores[key] = scores.get(key, 0) + 1 / (60 + rank)\nprint(sorted(scores, key=scores.get, reverse=True))|['b', 'a', 'c']|Reciprocal rank fusion adds contributions based on positions rather than raw similarity scales. Document b benefits from appearing in both lists. The rank constant controls how sharply top positions are favored.|Adding raw lexical and vector scores can mix incomparable scales.|Remove b from one ranking and predict how its combined position changes.
basic-rag|documents = {"returns": "Returns are accepted within 14 days."}\nquery = "When can I return an item?"\nselected = ["returns"]\ncontext = [documents[key] for key in selected]\nprint(context[0])\nprint("sources:", selected)|Returns are accepted within 14 days.\nsources: ['returns']|A retrieval stage selects evidence before a generator receives the question and context. This example models context assembly only; it makes no model call. Keeping source IDs alongside text enables later citation checks.|A correct-looking answer does not prove the retriever found the right evidence.|Use an empty selection and define an explicit insufficient-evidence response.
reranking|candidates = ["a", "b", "c"]\nquery_document_scores = {"a": 0.2, "b": 0.9, "c": 0.5}\nreranked = sorted(candidates, key=query_document_scores.get, reverse=True)\nprint(reranked)|['b', 'c', 'a']|These supplied scores model a second-stage reranker. A real cross-encoder scores each query-document pair jointly, then changes the candidate order. It cannot recover a document that the first-stage retriever omitted.|Reranking a poor candidate pool cannot create missing evidence.|Remove b before reranking and explain which retrieval stage must be improved.
query-rewriting|original = "How long do I have to send it back?"\nrewritten = "product return time limit"\nqueries = [original, rewritten]\nprint(len(queries))\nprint(queries[1])|2\nproduct return time limit|The rewrite makes likely retrieval terms explicit while retaining the original request for comparison. A real rewriter can drift from intent, so evaluate its retrieved evidence against a labeled question set.|A fluent rewrite can add assumptions the user never made.|Rewrite a query containing an exact product code without losing that code.
citations|sources = {"d1": "Returns are allowed for 14 days."}\nanswer = {"claim": "The return window is 14 days.", "citations": ["d1"]}\nprint(all(key in sources for key in answer["citations"]))|True|The membership check proves only that cited IDs exist in supplied evidence. A separate support check must determine whether each claim is actually justified by the cited text. Grounding is more than attaching a source-shaped label.|An existing citation can still fail to support its associated claim.|Change the claim to 90 days and explain why this structural check still passes.
retrieval-eval|relevant = {"a", "b", "c"}\nretrieved = ["a", "x"]\nhits = len(relevant.intersection(retrieved))\nprint(round(hits / len(retrieved), 2))\nprint(round(hits / len(relevant), 2))|0.5\n0.33|Precision measures how much retrieved material is relevant; recall measures how much relevant material was found. The denominators answer different questions. Use query-level labels and define empty-set behavior before aggregating.|A single aggregate score can hide failure on important query categories.|Compute both metrics after retrieving a, b, c and seven irrelevant documents.`;
export default rows;
