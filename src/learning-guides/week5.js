const rows=String.raw`base-model|from pydantic import BaseModel\nclass Item(BaseModel):\n    quantity: int\nitem = Item.model_validate({"quantity": "3"})\nprint(item.quantity, type(item.quantity).__name__)|3 int|Pydantic builds a model from external data and converts a compatible string to int under its default validation rules. The resulting field type is guaranteed by validation, unlike a plain annotation.|Coercion is a policy choice; strict mode rejects some values accepted by default.|Enable strict validation and try the same string input.
pydantic-fields|from pydantic import BaseModel, Field, ValidationError\nclass Page(BaseModel):\n    limit: int = Field(ge=1, le=20)\nprint(Page(limit=5).limit)\ntry:\n    Page(limit=0)\nexcept ValidationError as error:\n    print(error.errors()[0]["type"])|5\ngreater_than_equal|The field declares an inclusive range. Valid values enter the model; a violation produces structured error information rather than silently fixing the value. Defaults have their own validation configuration.|A default is not automatically validated in every Pydantic configuration.|Try the two endpoints and one value above the maximum.
field-validators|from pydantic import BaseModel, field_validator\nclass Person(BaseModel):\n    name: str\n    @field_validator("name")\n    @classmethod\n    def normalize(cls, value):\n        return value.strip().title()\nprint(Person(name=" ada ").name)|Ada|This after-validator receives a value that has already passed the field's ordinary type validation. It returns the normalized value that will be stored in the model. Returning nothing would replace it with None.|A validator must return the accepted or transformed value.|Reject a name that becomes empty after stripping.
model-validators|from pydantic import BaseModel, model_validator, ValidationError\nclass Window(BaseModel):\n    start: int\n    end: int\n    @model_validator(mode="after")\n    def ordered(self):\n        if self.end < self.start:\n            raise ValueError("end precedes start")\n        return self\ntry:\n    Window(start=5, end=2)\nexcept ValidationError:\n    print("invalid window")|invalid window|Each field can be a valid integer while their relationship is invalid. An after model validator sees the assembled model and enforces that relationship before returning self.|Independent field constraints cannot express every cross-field rule.|Allow equal endpoints, then explain whether that represents an empty or valid window in your domain.
nested-models|from pydantic import BaseModel\nclass Address(BaseModel):\n    city: str\nclass Person(BaseModel):\n    address: Address\nperson = Person.model_validate({"address": {"city": "Nis"}})\nprint(type(person.address).__name__)\nprint(person.address.city)|Address\nNis|The nested dictionary is validated against Address during construction of Person. Callers receive a typed nested object instead of repeatedly checking dictionary keys. Validation errors retain the path to a failing nested field.|Using dict everywhere discards the nested contract you intended to enforce.|Remove city and inspect the error location tuple.
discriminators|from typing import Annotated, Literal, Union\nfrom pydantic import BaseModel, Field, TypeAdapter\nclass Text(BaseModel):\n    kind: Literal["text"]\n    value: str\nclass Count(BaseModel):\n    kind: Literal["count"]\n    value: int\nMessage = Annotated[Union[Text, Count], Field(discriminator="kind")]\nitem = TypeAdapter(Message).validate_python({"kind": "count", "value": 3})\nprint(type(item).__name__)|Count|The kind tag selects the Count branch before its remaining fields are validated. This is more explicit than asking a validator to guess which similar shape was intended.|A discriminator must reliably identify the intended branch.|Use an unknown kind and compare its error with a wrong value type in the count branch.
serialization|from pydantic import BaseModel, Field\nclass Result(BaseModel):\n    request_id: str = Field(alias="requestId")\n    internal: str = Field(exclude=True)\nitem = Result(requestId="r2", internal="debug")\nprint(item.model_dump(by_alias=True))|{'requestId': 'r2'}|Validation accepts the external alias, while the model uses request_id internally. Serialization can emit aliases and exclude internal fields. Choose the outgoing contract explicitly rather than exposing every model attribute.|Successful validation does not make every field safe to return to clients.|Compare model_dump() with model_dump(by_alias=True).
json-schema|from pydantic import BaseModel, Field\nclass Search(BaseModel):\n    limit: int = Field(ge=1)\nschema = Search.model_json_schema()\nprint(schema["required"])\nprint(schema["properties"]["limit"]["minimum"])|['limit']\n1|The generated schema describes required fields and constraints for another component. It is a representation of a contract, not an execution engine or an authorization decision. Consumers must still validate requests and enforce policy.|A model-visible schema cannot enforce server-side permissions by itself.|Add an optional field and inspect how required changes.
agent-loop|tools = {"double": lambda value: value * 2}\nmodel_messages = iter([{"tool": "double", "value": 4}, {"answer": "done"}])\nfor message in model_messages:\n    if "answer" in message:\n        print(message["answer"])\n        break\n    print(tools[message["tool"]](message["value"]))|8\ndone|This deterministic model shows the control loop without a paid API: inspect a model message, dispatch an allowed tool, and stop on a final answer. A real runtime also feeds results back to the model and enforces budgets and validation.|A model proposes tool use; application code decides what may actually execute.|Introduce an unknown tool name and return a structured failure instead of dispatching it.
pydantic-agent|from pydantic_ai import Agent\nfrom pydantic_ai.models.function import FunctionModel\nfrom pydantic_ai.messages import ModelResponse, TextPart\ndef reply(messages, info):\n    return ModelResponse(parts=[TextPart(content="ready")])\nagent = Agent(FunctionModel(reply))\nresult = agent.run_sync("status?")\nprint(result.output)|ready|FunctionModel supplies a controlled model response so this real Agent example makes no external API request. Agent owns the run and exposes the final output. Use await agent.run inside an already-running async environment.|run_sync cannot start its own loop inside an active event loop.|Change the controlled response and inspect the recorded message history.
run-context|from dataclasses import dataclass\n@dataclass\nclass Dependencies:\n    tenant: str\n    records: dict\ndef lookup(deps, key):\n    return deps.records.get((deps.tenant, key), "missing")\ndeps = Dependencies("t1", {("t1", "a"): "allowed"})\nprint(lookup(deps, "a"))|allowed|This plain-Python model separates trusted per-run dependencies from model-supplied lookup arguments. In Pydantic AI, RunContext.deps carries that dependency object into tools. The model should not choose its own tenant authority.|Putting tenant authority in freely generated tool arguments can cross access boundaries.|Add a second tenant with the same record key and verify isolation.
agent-tools|def lookup_order(order_id: str) -> str:\n    """Return the state of one known order."""\n    return {"o7": "shipped"}.get(order_id, "missing")\nprint(lookup_order("o7"))\nprint(lookup_order("other"))|shipped\nmissing|The Python function is the behavior behind a tool. A framework exposes its name, documentation and argument schema, then calls it after validation. Keep its result bounded and useful for the next reasoning step.|A function exposed to a model still needs authorization and error handling at its boundary.|Register this function with Agent.tool_plain and inspect the generated schema in the linked documentation.
structured-output|from pydantic import BaseModel, ValidationError\nclass Decision(BaseModel):\n    approved: bool\n    reason: str\nprint(Decision.model_validate({"approved": False, "reason": "missing evidence"}).reason)\ntry:\n    Decision.model_validate({"approved": True})\nexcept ValidationError:\n    print("missing reason")|missing evidence\nmissing reason|This isolates the validation boundary used for structured agent output. A missing required field is rejected; a plausible but factually wrong reason can still validate. Retrying shape errors and evaluating truthfulness solve different problems.|Schema-valid output can still be incorrect or unauthorized.|Supply a false factual reason and explain what an evaluation would need to catch it.
streaming-history|chunks = ["The ", "order ", "shipped."]\npartial = ""\nfor chunk in chunks:\n    partial += chunk\nprint(partial)\nhistory = [{"role": "user", "text": "status?"}, {"role": "assistant", "text": partial}]\nprint(len(history))|The order shipped.\n2|This model distinguishes incremental transport from committed conversation state. The partial text grows during streaming; the completed turn is then recorded. Real tool histories need their call/result structure preserved as well.|A partial structured value should not trigger an irreversible action before final validation.|Stop after the first chunk and decide what should be saved as cancelled versus complete.
usage-limits|remaining_calls = 2\nfor request in ["first", "second", "third"]:\n    if remaining_calls == 0:\n        print("budget exhausted")\n        break\n    remaining_calls -= 1\n    print(request)|first\nsecond\nbudget exhausted|The budget check happens before admitting another operation. Tokens, model requests, tool calls, time and cost require distinct accounting. This local model demonstrates the policy without consuming an API budget.|A retry is still work and must consume the same end-to-end budget.|Give each request a different cost and enforce both a call count and a cost limit.
agent-tests|def run_agent(model):\n    request = model("status")\n    return request["answer"].upper()\ndef fake_model(prompt):\n    return {"answer": "ready"}\nprint(run_agent(fake_model))|READY|A deterministic model substitute makes the application path repeatable. In Pydantic AI, FunctionModel serves this role with real message objects. This proves transformation and wiring, not the quality of a live model's reasoning.|A passing fake-model test is not evidence that a real model chooses good tools.|Make the substitute return malformed output and verify the failure path separately.`;
export default rows;
