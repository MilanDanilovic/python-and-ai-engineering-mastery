const rows=String.raw`tool-schemas|schema = {"type": "object", "properties": {"order_id": {"type": "string"}}, "required": ["order_id"], "additionalProperties": False}\ntool = {"name": "lookup_order", "description": "Read an order's delivery state.", "inputSchema": schema}\nprint(tool["name"])\nprint(tool["inputSchema"]["required"])|lookup_order\n['order_id']|The declaration describes one bounded capability and its inputs. The required array and additionalProperties policy make the boundary explicit. The protocol transports this description; the server must enforce it when called.|A vague tool description makes even a correct schema hard for a model to select.|Add an optional include_items boolean and explain whether it changes authorization.
tool-validation|def validate(arguments):\n    if not isinstance(arguments.get("order_id"), str):\n        return {"ok": False, "error": "order_id must be text"}\n    return {"ok": True}\nprint(validate({"order_id": 7}))|{'ok': False, 'error': 'order_id must be text'}|Validate at the execution boundary even if the caller saw a schema. This small application result can be adapted to MCP tool content and isError when appropriate. Protocol failures and domain/tool failures need distinct handling.|Do not send a raw stack trace or secret-bearing exception to a model as ordinary tool output.|Add a length limit and an unknown-field policy.
tool-effects|completed = {}\ndef create_note(key, text):\n    if key not in completed:\n        completed[key] = {"id": len(completed) + 1, "text": text}\n    return completed[key]["id"]\nprint(create_note("req-1", "hello"))\nprint(create_note("req-1", "hello"))|1\n1|The in-memory model reuses a stored result when the same request is repeated. A real service needs atomic persistent storage, payload consistency checks and downstream idempotency to survive races and crashes.|This dictionary demonstration is not concurrency-safe durable deduplication.|Reuse req-1 with different text and define the rejection rule.
tool-permissions|def can_write(granted_scopes, record_tenant, caller_tenant):\n    return "notes:write" in granted_scopes and record_tenant == caller_tenant\nprint(can_write({"notes:read"}, "a", "a"))\nprint(can_write({"notes:write"}, "b", "a"))\nprint(can_write({"notes:write"}, "a", "a"))|False\nFalse\nTrue|Capability scope and resource ownership are separate conditions. Passing one does not satisfy the other. Trusted identity and grant information must come from authentication infrastructure, not a model's claim.|Hiding a write tool in the UI is not a substitute for checking authorization when it executes.|Add an expired-grant condition and explain where its timestamp must come from.
mcp-roles|host = "learning assistant"\nclients = {"documents": "document-server", "issues": "issue-server"}\nprint(host)\nfor client, server in clients.items():\n    print(client, "->", server)|learning assistant\ndocuments -> document-server\nissues -> issue-server|This diagram expressed as data separates the coordinating host from its protocol clients and the servers they call. One host can connect to several servers with different capabilities and trust boundaries.|A server exposing tools is not necessarily the agent that decides when to use them.|Draw where authentication and user consent belong for each connection.
mcp-discovery|catalog = [{"name": "lookup_order"}, {"name": "search_docs"}]\ndiscovered = {tool["name"] for tool in catalog}\nprint("search_docs" in discovered)\nprint("delete_all" in discovered)|True\nFalse|A tools/list response tells a client what a server currently exposes. The client can then construct a tools/call request for a selected capability. Current and older MCP revisions differ in initialization and capability negotiation; read the versioned reference.|A cached tool list can become stale, and discovery is not permission to execute every listed tool.|Handle pagination and a later tool-list change without silently retaining removed tools.
mcp-primitives|capabilities = {"tool": "search_orders", "resource": "policy://returns", "prompt": "summarize_order"}\nfor kind, name in capabilities.items():\n    print(kind, name)|tool search_orders\nresource policy://returns\nprompt summarize_order|Tools perform operations, resources expose addressable content, and prompts provide reusable interaction templates. These are different protocol primitives even when all contribute context to the same agent.|Treating every content read as a mutating tool blurs the capability model.|Classify a refund action, a product manual and a reusable support template.
mcp-transport|import json\nmessage = {"jsonrpc": "2.0", "id": 7, "method": "tools/list"}\nwire = json.dumps(message).encode("utf-8")\nreceived = json.loads(wire)\nprint(received["id"], received["method"])|7 tools/list|This models message encoding only. A real transport also frames delivery, carries required protocol metadata and defines cancellation. The linked transport binding gives those version-specific rules; authorization remains a separate boundary.|This minimal JSON object is not a complete current-version MCP request.|Compare how stdio framing and Streamable HTTP carry the same protocol method.
harness-runtime|allowed = {"read"}\nrequests = ["read", "delete"]\nfor request in requests:\n    print(request, "allowed" if request in allowed else "denied")|read allowed\ndelete denied|The harness is application code around the model: it chooses capabilities, runs or rejects requests, records results and enforces stop conditions. A persuasive model message cannot expand the allowlist by itself.|Instructions to the model are not an execution sandbox.|Add a two-call budget and a timeout policy to this dispatcher design.
harness-context|items = [{"text": "system rule", "trusted": True}, {"text": "retrieved page", "trusted": False}]\ncontext = [{"text": item["text"], "origin": "application" if item["trusted"] else "retrieval"} for item in items]\nprint([item["origin"] for item in context])|['application', 'retrieval']|Constructing context is a data-selection step. Provenance lets the application distinguish its instructions from untrusted retrieved content. A real implementation must preserve message roles, access filtering and token budgets too.|Concatenating all text into one unlabeled prompt erases useful trust boundaries.|Add a tenant ID and remove unauthorized content before constructing the context.
harness-observation|events = [{"run": "r1", "step": "model", "status": "ok"}, {"run": "r1", "step": "tool", "status": "timeout"}]\nprint([(event["step"], event["status"]) for event in events])|[('model', 'ok'), ('tool', 'timeout')]|A shared run identifier connects the decisions and effects of one attempt. Separate events preserve where a failure occurred. Real traces add parent/child spans, durations and controlled attributes.|Logging complete prompts and tool payloads by default can expose sensitive data.|Record attempt number and duration without storing the secret input.
ontology-entities|entities = {"customer:17": {"name": "Alex"}, "customer:28": {"name": "Alex"}}\nprint(len(entities))\nprint(entities["customer:17"]["name"])|2\nAlex|Stable identifiers distinguish entities even when display names collide. Labels can change without changing identity. The same identifier should be carried through storage, tools and retrieved evidence.|A human-readable name is rarely a safe unique key.|Rename one customer and show which links should remain unchanged.
ontology-relations|relations = [("order:9", "placed_by", "customer:17"), ("order:9", "contains", "product:4")]\nowner = [obj for subject, predicate, obj in relations if subject == "order:9" and predicate == "placed_by"]\nprint(owner)|['customer:17']|A relation has a source, a named meaning and a target. The graph representation alone does not enforce that an order has exactly one customer; that is a separate domain constraint.|Storing triples does not automatically enforce cardinality or referential integrity.|Add a second placed_by relation and decide whether it is invalid or represents a different domain model.
ontology-contracts|record = {"customer_id": "customer:17", "evidence_id": "document:3"}\ntool_input = {"customer_id": record["customer_id"]}\ncitation = {"source": record["evidence_id"], "about": record["customer_id"]}\nprint(tool_input["customer_id"] == citation["about"])|True|The same domain identifier connects the action contract and its supporting evidence. A shared vocabulary prevents one subsystem from interpreting customer as a name while another expects a stable ID.|A common field spelling is not enough if systems assign different meaning or units to it.|Write a contract distinguishing customer_id, account_id and document_id.`;
export default rows;
