import React from 'react';
import {weeks,resources} from './data';
const labs=[
 {why:'Small data transformations are the foundation of dependable Python services. Predicting values and side effects is more useful than remembering syntax.',explain:'A Python name refers to an object. Containers hold references to other objects. Operations may return a new value or mutate an existing object; understand which behavior each API promises. Keep transformations explicit and validate the shape of external data.',code:'events = [{"level": "error"}, {"level": "info"}]\ncounts = {}\nfor event in events:\n    level = event["level"]\n    counts[level] = counts.get(level, 0) + 1\nprint(counts)',debug:'def record(event, events=[]):\n    events.append(event)\n    return events',question:'Why can a default list retain data across function calls?',hard:'Build a log aggregation pipeline that handles malformed input and nested records without a framework.'},
 {why:'Python protocols explain the behavior hidden behind framework APIs, decorators, and resource management.',explain:'Protocols connect syntax to methods. Iteration asks for an iterator, then repeatedly requests the next item until StopIteration. A generator retains its local state between yields. Decorators receive a callable and return the value bound to its name. Composition lets small objects collaborate without a deep inheritance hierarchy.',code:'def errors(events):\n    for event in events:\n        if event["level"] == "error":\n            yield event\n\nstream = errors([{"level": "error"}])\nprint(next(stream))',debug:'stream = (n * 2 for n in range(3))\nprint(list(stream))\nprint(list(stream))',question:'Why is the second list empty, and how would you make the data reusable?',hard:'Combine a context manager, a generator pipeline, and a dataclass domain model in the log analyzer.'},
 {why:'Clear imports, types, and tests make a Python system understandable when its original author is unavailable.',explain:'A module is an importable unit with its own namespace. Separate domain logic from I/O so tests can control dependencies. Type annotations communicate contracts and support static checking; Python does not generally enforce them at runtime. Tests should describe observable behavior and failure cases.',code:'def error_rate(errors: int, total: int) -> float:\n    if total <= 0:\n        raise ValueError("total must be positive")\n    return errors / total\n\ndef test_error_rate():\n    assert error_rate(1, 4) == 0.25',debug:'def parse_count(value: int) -> int:\n    return value\n\nprint(parse_count("three"))',question:'Why does an annotation alone not reject this string?',hard:'Create an installable src-layout package, inject configuration, and test both success and failure cases.'},
 {why:'Concurrency only helps when the system can make progress while another operation waits.',explain:'An event loop schedules cooperative tasks. Await suspends the current coroutine when the awaited operation needs to wait, allowing other tasks to run. Blocking calls keep the event-loop thread busy. Bound concurrency, set timeouts, and let cancellation propagate so resource cleanup can finish.',code:'import asyncio\n\nasync def work(value):\n    await asyncio.sleep(0.1)\n    return value * 2\n\nasync def main():\n    return await asyncio.gather(work(1), work(2))\n\nprint(asyncio.run(main()))',debug:'async def handler():\n    import time\n    time.sleep(5)\n    return "done"',question:'What happens to other tasks during time.sleep, and where should blocking work run?',hard:'Build a request runner with bounded concurrency, timeouts, and cancellation cleanup. Compare it with sequential execution.'},
 {why:'Typed boundaries make external data and model outputs easier to validate, inspect, and test.',explain:'Pydantic validates input against a declared model and reports structured validation errors. An agent coordinates model requests, tool calls, and the final result. Structured output constrains shape; it does not establish factual correctness or permission to perform a side effect. Keep business rules and authorization explicit.',code:'from pydantic import BaseModel, Field\n\nclass SupportNote(BaseModel):\n    customer_id: int = Field(gt=0)\n    text: str = Field(min_length=1)\n\nnote = SupportNote(customer_id=12, text="Follow up")\nprint(note.model_dump())',debug:'payload = {"customer_id": -1, "text": ""}\nnote = SupportNote.model_construct(**payload)',question:'Why does bypassing validation at an external boundary undermine the model contract?',hard:'Build the same tool-using assistant with a manual model loop and Pydantic AI. Compare validation, retries, and message history.'},
 {why:'Reliable agents need precise interfaces and explicit boundaries between reasoning and execution.',explain:'A tool describes an operation with an input contract and an execution function. MCP standardizes discovery and access between hosts, clients, and servers. A harness supplies the execution environment, tool runtime, context, and permissions. An ontology defines domain entities and their relationships so operations refer to consistent concepts.',code:'tool = {\n    "name": "get_customer_orders",\n    "description": "Read orders belonging to a customer",\n    "input_schema": {"customer_id": "positive integer"},\n    "effect": "read-only",\n}\n# Validate input and authorize access before executing.',debug:'# A tool receives customer_id from the model.\n# It queries all orders without checking caller access.',question:'Why does valid input not imply that the caller is authorized?',hard:'Implement three customer-support tools, expose them through MCP, and document permissions and idempotency for writes.'},
 {why:'Retrieval quality limits how well a grounded answer can be supported by evidence.',explain:'RAG separates evidence retrieval from answer generation. Ingestion parses and chunks documents while retaining source metadata. Retrieval selects candidates; reranking refines their order. Context construction selects evidence within a budget. Evaluate retrieval independently so missing evidence is not confused with poor generation.',code:'def precision_at_k(retrieved, relevant, k):\n    selected = retrieved[:k]\n    if not selected:\n        return 0.0\n    return sum(item in relevant for item in selected) / len(selected)\n\nprint(precision_at_k(["a", "b"], {"b"}, 2))',debug:'# A relevant passage exists in storage.\n# The retriever returns unrelated chunks.\n# The team changes only the generation prompt.',question:'Which pipeline stage should be inspected first, and what evidence would isolate the failure?',hard:'Compare vector, full-text, and hybrid retrieval on a fixed dataset, including questions with no supported answer.'},
 {why:'Production correctness includes what happens after partial success, interruption, and recovery.',explain:'Retries can repeat side effects. An idempotency key lets an operation recognize an earlier request and reuse its result. Durable workflows record progress so interrupted work can resume. Observe individual steps, evaluate outputs against datasets, and enforce authorization at the point where a tool executes.',code:'def apply_once(key, payload, store):\n    # Illustrative only: production needs an atomic transaction.\n    if key in store:\n        return store[key]\n    result = {"status": "accepted", "payload": payload}\n    store[key] = result\n    return result',debug:'# A payment succeeds, but its response is lost.\n# A retry sends the same payment with a new request ID.',question:'How could this create duplicate effects, and what must be persisted atomically?',hard:'Interrupt a multi-step workflow after a successful side effect. Recover without duplicating it and record traces, cost, and evaluation results.'}
];
export function LessonContent({topic}){const wi=Math.max(0,weeks.findIndex(w=>w.concepts.includes(topic)));const lab=labs[wi];const objects=/mutab|copy|identit|reference|object model/i.test(topic);return <><h3>Why it matters</h3><p>{lab.why}</p><h3>Concept explanation</h3><p>{objects?'Assignment binds a name to an object; it does not copy the object. A shallow copy creates a new outer container while retaining references to nested values. Mutating a shared nested object is visible from both containers. Equality compares values; identity asks whether two references point to the same object.':lab.explain}</p><p className="callout">Today’s investigation: <strong>{topic}</strong>. Locate its contract in the official reference, then demonstrate both expected behavior and an edge case.</p><h3>Official documentation</h3><a target="_blank" rel="noreferrer" href={objects?'https://docs.python.org/3/library/copy.html':resources[[0,0,2,3,4,6,8,9][wi]][1]}>{objects?'Python: shallow and deep copy':resources[[0,0,2,3,4,6,8,9][wi]][0]} ↗</a><p>Additional reading: <a target="_blank" rel="noreferrer" href={resources[[1,1,11,10,5,6,7,9][wi]][1]}>{resources[[1,1,11,10,5,6,7,9][wi]][0]} ↗</a></p><h3>Small example · predict before running</h3><pre>{objects?'a = [[1, 2], [3, 4]]\nb = a.copy()\nb[0].append(5)\nprint(a)\nprint(a is b)\nprint(a[0] is b[0])':lab.code}</pre><h3>Three exercises</h3><ol><li>Implement the smallest working example of {topic} using the documentation.</li><li>Add an empty, invalid, or boundary input. Write down the expected result before running it.</li><li>Write a test that verifies the behavior, then explain why it passes.</li></ol><h3>Difficult exercise</h3><p>{lab.hard}</p><h3>Practical engineering problem</h3><p>{weeks[wi].project}. Apply {topic} and document the tradeoff you made.</p><h3>Debugging challenge</h3><pre>{lab.debug}</pre><p>{lab.question}</p><h3>Code reading challenge</h3><p>Trace the example above step by step. Identify where values, references, control flow, or side effects change. Compare your prediction with the actual result.</p><h3>Explain it yourself</h3><p>Explain {topic} to another engineer without using an AI agent. What does it guarantee, what does it not guarantee, and when would you use it?</p></>}
